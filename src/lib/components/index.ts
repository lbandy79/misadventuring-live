/**
 * `src/lib/components` — Reusable interaction primitives.
 *
 * Phase 3c: VoteSubmission only. The show-specific components (monster
 * builder, decoder ring, ship combat) remain colocated under
 * src/components until they're either rewritten generically or extracted
 * piecemeal.
 */
export { VoteSubmission, type VoteSubmissionProps } from './VoteSubmission';
