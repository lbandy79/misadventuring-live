/**
 * Phase 2c shim — original module relocated to `src/lib/audio-fx/sounds`.
 * Prefer importing from `src/lib/audio-fx` in new code.
 */
export * from '../lib/audio-fx/sounds';
export { default } from '../lib/audio-fx/sounds';
